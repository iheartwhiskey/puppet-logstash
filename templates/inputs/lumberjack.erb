input {
    lumberjack {
        port            => <%= @port %> 
        ssl_certificate => "<%= @certificate %>"
        ssl_key         => "<%= @key %>"
        type            => "lumberjack"
    }
}
